<head>
  <title>Edopter</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>


	<nav>
		<ul class="nav nav-pills pull-right">
		<li class="active"><%= link_to "Home",home_url  %> </li>
			<li><% if session[:user_id].present? %>
       			<%= link_to "Sign Out", sessions_url, method: 'delete' %>
     			<% else %>
       			<li><%= link_to "Sign In", login_nav_url %> </li>
       			<li><%= link_to "Sign Up", start_url %> </li>
     			<% end %>
          
			<li><%= link_to "What's Next", events_url %> </li>
			<li class="disabled"><a href="#">30 Day!</a></li>
		</ul>
	</nav>	


    <% if session[:user_id].present? %>
      <p>Welcome back, <%= User.find(session[:user_id]).username %>!</p>
    <% end %>



<%= yield %>

</body>